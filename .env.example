# Environment variables for Odoo Scientific Project Manager
# Copy this file to .env and update with your actual values
# IMPORTANT: Never commit .env to version control!

# Database Configuration
DB_PASSWORD=your_strong_password_here
DB_USER=odoo_user
DB_NAME=scientific_db

# Security Notes:
# 1. Use a strong password (min 16 characters, mixed case, numbers, special chars)
# 2. Change these default values in production
# 3. Keep your .env file secure and never share it
# 4. Use different credentials for development and production

# Example strong password generation (Linux/Mac):
# openssl rand -base64 32
