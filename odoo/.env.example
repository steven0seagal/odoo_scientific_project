# Database Configuration
# Copy this file to .env and update with your actual credentials
# NEVER commit the .env file to version control!

# PostgreSQL Database Credentials
DB_PASSWORD=your_secure_password_here
DB_USER=odoo
DB_NAME=postgres

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Generate a strong password and replace 'your_secure_password_here'
# 3. Update other values as needed
# 4. The .env file is excluded from git by .gitignore

# Security Note:
# - Use a strong, randomly generated password (minimum 16 characters)
# - Never use default passwords in production
# - Keep your .env file secure and never share it
